#include <stdio.h>

int main() {
    int a = 10, b = 5;
    int max;

    // Using conditional operator (ternary operator)
    max = (a > b) ? a : b;

    printf("Maximum value between %d and %d is %d\n", a, b, max);

    return 0;
}
